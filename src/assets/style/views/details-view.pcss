.detail-page {
    .commits-head {
        @apply flex flex-wrap justify-between items-center gap-4 py-4;

        &__title {
            
        }

        &__search {
            @apply border border-slate-500 p-4 w-full;
            @apply md:max-w-[400px];
        }
    }
    

    .commits-list {
        

        .commit {
            @apply flex items-center gap-5 px-6 py-3 border-b border-slate-300 dt-colors;
        }
    }
}