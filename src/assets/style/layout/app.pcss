.profile {
    @apply flex flex-col items-center mb-8;
    @apply nm:mb-16;

    &__image {
        @apply w-32 mb-4 rounded-md;
    }

    &__bio {
        @apply text-center;
    }
}

.page-content {
    @apply col-span-main;
    @apply nm:col-start-main-3 nm:col-span-8;
}