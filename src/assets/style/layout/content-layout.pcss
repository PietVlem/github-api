.content-layout {
    &__breadcrumb {
        @apply flex flex-wrap items-center gap-x-4 gap-y-2 mb-8 text-sm text-slate-500;
        a {
          @apply dt-colors;
          &:hover { @apply text-cyan-600; }  
        }
    }

    &__title {
        @apply pb-4 border-b border-slate-500;
    }
}